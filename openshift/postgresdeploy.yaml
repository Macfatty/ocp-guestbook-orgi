apiVersion: v1
kind: Secret
metadata:
  name: ayuub-postgres-secret
type: Opaque
stringData:
  POSTGRESQL_DATABASE: guestbook
  POSTGRESQL_USERNAME: guest
  POSTGRESQL_PASSWORD: ocp123
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ayuub-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: lvms-vg1
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ayuub-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ayuub-postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: docker.io/bitnamilegacy/postgresql:16.6.0-debian-12-r2
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRESQL_DATABASE
              valueFrom:
                secretKeyRef:
                  name: ayuub-postgres-secret
                  key: POSTGRESQL_DATABASE
            - name: POSTGRESQL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: ayuub-postgres-secret
                  key: POSTGRESQL_USERNAME
            - name: POSTGRESQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ayuub-postgres-secret
                  key: POSTGRESQL_PASSWORD
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: postgres-data
              mountPath: /bitnami/postgresql
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: ayuub-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ayuub-postgres
spec:
  selector:
    app: ayuub-postgres
  ports:
    - name: tcp-5432
      port: 5432
      targetPort: 5432

