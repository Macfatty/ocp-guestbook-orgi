apiVersion: apps/v1
kind: Deployment
metadata:
  name: ayuub-guestbook-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guestbook-backend
  template:
    metadata:
      labels:
        app: guestbook-backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/macfatty/ocp-guestbook-orgi-backend:3473d736a8ec5b2cba760f5a2eb28b43838ded34
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: DB_HOST
              value: postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: ayuub-postgres-secret
                  key: POSTGRESQL_DATABASE
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: ayuub-postgres-secret
                  key: POSTGRESQL_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ayuub-postgres-secret
                  key: POSTGRESQL_PASSWORD
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ayuub-backend
spec:
  selector:
    app: ayuub-guestbook-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080

